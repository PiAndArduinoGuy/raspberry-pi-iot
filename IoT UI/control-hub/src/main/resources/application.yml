server:
  port: 8080
  servlet:
    context-path: "/control-hub-backend"
spring:
  rabbitmq:
    port: 5672
  cloud:
    stream:
      bindings:
        newPumpConfigOutput:
          destination: pumpconfigupdate
        newAmbientTempInput:
          destination: ambienttemp
        pumpStateInput:
          destination: pumpcontrollertogglestatus
